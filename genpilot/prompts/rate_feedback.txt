Your goal is to judge whether the modified prompt improves image generation compared with the original prompt and previous errors.

You will receive:
- original prompt from round 1
- errors from round 1
- modified prompt from round 2
- generated image from round 2
- remaining errors from round 2

Score the modified prompt from 1 to 5 for each aspect:
- 1: strongly disagree
- 2: disagree
- 3: neutral
- 4: agree
- 5: strongly agree

Aspects:
- Attribute-Binding: whether object attributes such as color, shape, texture, number, material, and style are correctly bound to the right objects.
- Object-Relationship: whether spatial relationships, non-spatial relationships, actions, object presence, and counts are correct.
- Background-Consistency: whether the background, setting, atmosphere, lighting, and context match the intended prompt.

Return ONLY one fenced JSON block.
Do not include any text before or after the JSON block.
The JSON must be valid and must exactly follow this schema:

```json
{
  "scores": {
    "Attribute-Binding": 4,
    "Object-Relationship": 4,
    "Background-Consistency": 4
  },
  "reasons": {
    "Attribute-Binding": "One short sentence explaining the attribute score.",
    "Object-Relationship": "One short sentence explaining the relationship score.",
    "Background-Consistency": "One short sentence explaining the background score."
  }
}
```

Rules:
- Each score must be an integer from 1 to 5.
- Do not use arrays for scores.
- Do not use placeholders.
- Do not use trailing commas.
- Use double quotes for all JSON keys and string values.
- The top-level JSON object must contain only "scores" and "reasons".
- The keys inside "scores" and "reasons" must be exactly "Attribute-Binding", "Object-Relationship", and "Background-Consistency".

Input: {prompt}
